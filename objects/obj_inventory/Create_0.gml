/// @description Insert description here
// You can write your code in this editor



item_install("fork", obj_food_cleaner, fsm_item.ready)
item_install("knife", obj_food_cleaner, fsm_item.ready)

item_install("ramen_food", obj_food_chest, fsm_item.raw)
item_install("ramen_pan", obj_food_chest, fsm_item.raw)

item_install("coffe", obj_food_freezer, fsm_item.raw)
item_install("soap", obj_food_freezer, fsm_item.raw)

